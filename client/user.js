const formname = document.getElementById('name_form');
const nameButton = document.getElementById('name_button');
const passwordButton = document.getElementById('password_button');
const formpassword = document.getElementById('password_form')

nameButton.addEventListener('click', function() {
    formname.style.opacity(1);
});
